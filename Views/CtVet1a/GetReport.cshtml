@model nadis.Models.Reports
@{
    ViewData["Title"] = "Report ctVet1a";
}
<a class="btn btn-danger"   asp-controller="CtVet1a"
                            asp-action="GetReportInPdf"
                            asp-route-dt="@Model.dt">PDF</a>
<p></p>
<div class="container">
    <div class="webreport">
        @await Model.report.Render()
    </div>
</div>